# Lost-Academy

```
javascript:(async()=>{if(window._somma_boot)return;window._somma_boot=true;const CONFIG={script:"https://raw.githubusercontent.com/Somma11/Lost-Academy/refs/heads/main/LostAcademy.js",sound:"https://raw.githubusercontent.com/Somma11/Lost-Academy/refs/heads/main/boot.wav",retries:3};const log=(...a)=>console.log("[SOMMA]",...a);async function playBootSound(){try{const audio=new Audio(CONFIG.sound+"?v="+Date.now());audio.volume=0.6;await audio.play().catch(()=>{});await new Promise(r=>{audio.onended=r;setTimeout(r,2000); });}catch(e){log("boot sound fail",e);}}function loadScriptTag(src){return new Promise((res,rej)=>{const s=document.createElement("script");s.src=src+"?v="+Date.now();s.async=true;s.onload=res;s.onerror=rej;(document.head||document.documentElement).appendChild(s);});}async function loadFetch(src){const r=await fetch(src+"?v="+Date.now(),{cache:"no-store"});const t=await r.text();(0,eval)(t);}async function loadBlob(src){const r=await fetch(src+"?v="+Date.now(),{cache:"no-store"});const t=await r.text();const blob=new Blob([t],{type:"text/javascript"});const url=URL.createObjectURL(blob);await loadScriptTag(url);}async function loadWithRetry(){for(let i=0;i<CONFIG.retries;i++){try{await loadScriptTag(CONFIG.script);log("loaded via tag");return;}catch{}try{await loadBlob(CONFIG.script);log("loaded via blob");return;}catch{}try{await loadFetch(CONFIG.script);log("loaded via fetch");return;}catch{}}console.error("[SOMMA] loader failed");}await playBootSound();await loadWithRetry();})();
```
